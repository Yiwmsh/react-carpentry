import styled from "@emotion/styled";
import { ScrollToButton } from "../Inputs/Button/ScrollToButton/ScrollToButton";

const StickyAnchor = styled(ScrollToButton)`
  position: sticky;
  top: 1em;
  bottom: 1em;
`;
